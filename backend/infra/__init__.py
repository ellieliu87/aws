"""Infrastructure provisioning for the workbench's AWS-backed paths.

Idempotent, re-runnable scripts that create and tear down the resources the
app needs. Versioned here so the deployed shape is reviewable in the repo
rather than living only in someone's console history.
"""
